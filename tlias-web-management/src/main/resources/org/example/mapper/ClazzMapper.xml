<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.ClazzMapper">
    <select id="list" resultType="org.example.pojo.Clazz">
        SELECT
        c.id,
        c.name,
        c.room,
        c.begin_date,
        c.end_date,
        e.id AS master_id,
        e.name AS master_name,
        c.create_time,
        c.update_time,
        CASE
        WHEN CURDATE() &lt; c.begin_date THEN '未开班'
        WHEN CURDATE() BETWEEN c.begin_date AND c.end_date THEN '已开班'
        WHEN CURDATE() &gt; c.end_date THEN '已结课'
        END AS status
        FROM clazz c
        LEFT JOIN emp e ON c.master_id = e.id
        <where>
            <if test="name != null and name != ''">
                AND c.name LIKE CONCAT('%', #{name}, '%')
            </if>

            <!--
                时间筛选（课程/班级开班时间区间）
                语义：班级日期区间 [begin_date, end_date] 与筛选区间 [begin, end] 有交集
                - 只传 begin：筛选 end_date >= begin（在 begin 之后仍未结束的班级）
                - 只传 end：  筛选 begin_date <= end（在 end 之前已经开始的班级）
                - begin+end：  end_date >= begin AND begin_date <= end
            -->
            <if test="begin != null">
                AND c.end_date &gt;= #{begin}
            </if>
            <if test="end != null">
                AND c.begin_date &lt;= #{end}
            </if>
        </where>
        ORDER BY c.update_time DESC
    </select>
</mapper>