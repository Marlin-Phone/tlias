<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.StudentMapper">
    <delete id="delete" parameterType="java.util.List">
        DELETE FROM student WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="list" resultType="org.example.pojo.Student">
        <!-- 动态sql -->
        SELECT s.*, c.name as clazz_name
        FROM student s
        LEFT JOIN clazz c ON s.clazz_id = c.id
        <where>
            <!-- 动态条件拼接（自动处理 AND/OR 连接） -->
            <if test="name != null and name != ''">
                AND s.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="degree != null">
                AND s.degree = #{degree}
            </if>
            <if test="clazzId != null">
                AND s.clazz_id = #{clazzId}
            </if>
        </where>
        ORDER BY s.update_time DESC
    </select>
    <select id="countStudentDegreeData" resultType="java.util.Map">
        select
            (case when degree = 1 then '初中'
                  when degree = 2 then '高中'
                  when degree = 3 then '大专'
                  when degree = 4 then '本科'
                  when degree = 5 then '硕士'
                  when degree = 6 then '博士'
                  else '其他' end) as name,
            count(*) as value
        from student group by degree order by degree desc;
    </select>
    <select id="countStudentData" resultType="java.util.Map">
        select clazz.name as clazzList, count(*) as dataList
        from student
            left join clazz on student.clazz_id = clazz.id
        group by clazz_id order by dataList
    </select>
</mapper>